export const Roles = {
    user:"user",
    worker:"worker",
    admin:"admin",
    customer: "user" // Or however they want to map it, I'll use 'user' as base but let them define it.
}
export const OtpTypesEnum={
    CONFIRMATION:"confirm",
    RESETPASSWPRD:"resetPassword"
}